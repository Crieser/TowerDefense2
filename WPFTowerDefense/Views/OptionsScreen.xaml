<UserControl x:Class="WPFTowerDefense.Views.OptionsScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFTowerDefense.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="LightGray">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" Margin="20">
            <TextBlock Text="{Binding VolumeText}" FontSize="20" HorizontalAlignment="Center"/>

            <Slider Minimum="0"
                    Maximum="1"
                    TickFrequency="0.05"
                    IsSnapToTickEnabled="True"
                    LargeChange="10"
                    SmallChange="1"
                    Margin="10"
                    Value="{Binding VolumeLevel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <Button Content="Return" Command="{Binding ReturnCommand}" Height="40"/>
        </StackPanel>
    </Grid>
</UserControl>
