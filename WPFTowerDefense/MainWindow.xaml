<Window x:Class="WPFTowerDefense.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFTowerDefense"
        mc:Ignorable="d"
        Title="Tower Defense" Width="1295" Height="775" 
        MinWidth="1295" MinHeight="775"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <ContentControl Content="{Binding CurrentView}" />

        <Rectangle Fill="Black"
                   Opacity="0.5"
                   Visibility="{Binding IsDimOverlayVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                   IsHitTestVisible="False"
                   Panel.ZIndex="10"/>
    </Grid>
</Window>
